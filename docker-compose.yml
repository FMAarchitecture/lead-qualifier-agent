version: "3.8"

services:
  lead-qualifier-agent:
    build: .
    container_name: lead-qualifier-agent
    ports:
      - "8080:8080"
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - SERPAPI_API_KEY=${SERPAPI_API_KEY}